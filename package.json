{
	"name": "chesleybrown-resume",
	"description": "Single page, printable resumé for Chesley Brown written in Markdown.",
	"version": "0.0.2",
	"private": true,
	"engines": {
		"node": "18.17.1"
	},
	"devDependencies": {
		"markdown-styles": "^3.2.0"
	},
	"scripts": {
		"generate": "generate-md --layout mixu-page --input index.md --output ./dist"
	}
}
